

Most of your nodes were accepted. These problems remain:
{problems}
Return ONLY the corrected nodes for these ids: {node_ids}. Do not return any other
node. Assertions may only call: {allowed_functions}, plus the dict methods
.get/.keys/.values/.items, and may read `structured`, `context` and
`ancestors['<ancestor_id>']['<key>']`. Keep every part of a node that had no problem exactly
as it was.
