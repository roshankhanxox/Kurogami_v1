
Write your full answer as prose first. Wherever you state a figure, say where it
comes from: the URL of the search result it came from, or the id of the ancestor
it came from in square brackets, e.g. [market_sizing]. If it is your own estimate,
say "estimate" and show the arithmetic it rests on.

Then end your response with a fenced JSON code block (```json ... ```). The JSON is
a short, machine-readable summary of your answer -- never put the answer itself, or
long passages of prose, inside it.
{key_rules}
Optional: if answering properly required a question that nobody upstream
answered -- it is not covered anywhere in the context above -- add a top-level key
"missing_prerequisites" listing those questions. Leave it out otherwise. Do not
list things you can reasonably estimate yourself.
